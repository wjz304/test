name: Python CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      #- name: Set up Python
      #- uses: actions/setup-python@v5
      #  with:
      #    python-version: '3.11' 

      - name: Verify Python version
        run: |
          python --version
          python3 --version

